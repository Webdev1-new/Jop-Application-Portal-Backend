apiVersion: v1
kind: Service

metadata:
  name: jobportal-service
  namespace: jobportal

spec:

  selector:
    app: jobportal

  ports:

    - port: 8080
      targetPort: 8080
      nodePort: 30080

  type: NodePort